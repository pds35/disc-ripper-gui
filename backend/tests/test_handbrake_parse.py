"""
Tests for HandBrake output parsing.

Empty for now (Phase 0) — real tests land in Phase 3, once we have a
--json HandBrake log to develop the parser against. See sample-logs/
for the fixture files these tests will read from.
"""


def test_placeholder():
    """Placeholder so pytest has something to collect in Phase 0."""
    assert True
